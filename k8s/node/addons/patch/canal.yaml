kind: DaemonSet
metadata:
  name: canal
  namespace: kube-system
spec:
  template:
    spec:
      containers:
      - name: kube-flannel
        env:
        - name: FLANNELD_IFACE_REGEX_TIRORI
          value: ^10\.0\.
